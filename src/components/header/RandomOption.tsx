import styled from "styled-components"

const Container = styled.div`

`

const RandomOptionItem = styled.div`
  
`

export default function Header() {
  return (
    <Container> 
      
    </Container>
  );
}